# 菜单管理模块“数据库结构”说明（纯前端版）

本项目为纯静态站点，未连接服务器数据库。“数据库”指两部分：

1) 线上可发布的数据源（静态 JSON 文件）
2) 用户浏览器内的持久化存储（localStorage）

---

## 1. 数据源（静态 JSON）

通过 [data/manifest.json](file:///c:/Users/阳光宅男/Desktop/AI提示词/data/manifest.json) 声明数据源清单，页面启动时会按清单加载并解析各食堂菜单文件。

### 1.1 manifest 结构

```json
{
  "sources": [
    { "canteen_id": "二食堂", "file": "./data/mishu-leifan.json" },
    { "canteen_id": "一食堂", "file": "./一食堂.json" },
    { "canteen_id": "三食堂", "file": "./三食堂.json" }
  ]
}
```

- canteen_id：食堂标识（推荐隔离与分层展示的核心字段）
- file：相对站点根目录的 JSON 路径

### 1.2 支持的菜单 JSON 格式（导入/内置加载通用）

- 三食堂格式：根字段为 `食堂窗口信息`（窗口 → 分类 → 菜品列表）
- 一食堂格式：根字段为 `档口列表`（档口/分区/对象映射/数组混合）
- 简单菜单格式：根字段为 `菜品列表`（菜品名称/价格/单位）

---

## 2. 统一后的菜品数据结构（Dish）

页面会把不同格式的 JSON 解析为统一结构，形成“菜品表”。字段如下：

```json
{
  "key": "三食堂::楚汉风味::红烧牛肉土豆粉(面)",
  "name": "红烧牛肉土豆粉(面)",
  "price": 13,
  "price_label": "¥13",
  "window_id": "1号",
  "window_name": "楚汉风味",
  "canteen_id": "三食堂",
  "category": "汤粉汤面",
  "nutrition_info": {
    "calories": 412,
    "protein": 19,
    "fat": 22,
    "carbs": 35,
    "source": "《中国食物成分表 第6版》"
  },
  "meal_period": "午餐/晚餐",
  "store_name": "三食堂 · 楚汉风味 · 汤粉汤面",
  "unit": "元/份",
  "source_file": "三食堂.json"
}
```

字段约束（用于验证/清洗）：
- 必填：`key`、`name`、`canteen_id`、`window_name`、`nutrition_info`
- `price`：number 或 null；导入时会从字符串价格中提取数值（如 `"6元/份"` → 6）
- `meal_period`：缺失时自动推断（早餐/午餐/晚餐/夜宵），无法推断则为 `午餐/晚餐`

---

## 3. 导入批次记录结构（ImportBatch）

每次“内置加载”或“本地上传导入”都会记录一条导入批次，用于版本追溯与错误反馈。

```json
{
  "batch_id": "1715000000000_ab12cd",
  "imported_at": "2026-05-06T12:34:56.789Z",
  "source": "三食堂.json, 一食堂.json",
  "source_label": "本地上传",
  "inserted": 1200,
  "updated": 35,
  "skipped": 10,
  "errors": 3,
  "error_items": [
    { "message": "JSON解析失败：Unexpected token ...", "file": "xxx.json", "sample": null }
  ]
}
```

---

## 4. 浏览器存储（localStorage）

纯前端版使用 localStorage 作为“本机数据库”：

- `nutrition_dishes_v2`：统一后的菜品数组（Dish[]）
- `nutrition_import_batches_v2`：导入批次数组（ImportBatch[]）

兼容迁移：
- 若检测到旧版本 `nutrition_dishes_v1 / nutrition_import_batches_v1`，会自动迁移到 v2。

---

## 5. 推荐算法的食堂隔离规则

推荐时严格使用 `profile.canteen_id` 对候选菜品进行过滤：

- 候选集：`menu.filter(d => d.canteen_id === profile.canteen_id)`
- 推荐结果校验：若结果中存在 `canteen_id` 不一致的菜品，直接报错并提示

跨窗口推荐允许，但必须保持 `canteen_id` 一致。 

